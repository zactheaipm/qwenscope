model_id: "Qwen/Qwen3.5-35B-A3B"
hidden_dim: 2048
num_layers: 40
block_size: 4  # 3 DeltaNet + 1 Attention per block
num_blocks: 10
context_length: 4096  # Working context for experiments (not max 262144)
dtype: "bfloat16"

# MoE parameters (informational — used by architecture analysis, not SAE training)
num_experts: 256
num_experts_per_tok: 8          # 8 routed experts
shared_expert: true              # +1 shared expert (9 active total)
moe_intermediate_size: 512       # per-expert FFN intermediate dim
shared_expert_intermediate_size: 512
